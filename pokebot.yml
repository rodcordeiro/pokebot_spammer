# yaml-language-server: $schema=https://raw.githubusercontent.com/rodcordeiro/pokebot_spammer/develop/.github/schema.json

authentication:
  user: 'xxx'
  password: 'xxxx'

actions:
  action: 'spamming'
  levels: 10
  multipages: false
